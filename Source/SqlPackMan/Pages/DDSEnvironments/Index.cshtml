@page
@model SqlPackMan.Pages.DDSEnvironments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DDSEnvironment[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DDSEnvironment[0].Connection)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DDSEnvironment[0].SourceDb)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DDSEnvironment[0].TargetDb)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DDSEnvironment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Connection)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SourceDb)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TargetDb)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
